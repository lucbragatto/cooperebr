# Relatório QA Leve — 17/05/2026 noite final

**Origem:** Main thread (subagent `cooperebr-qa-funcional` indisponível na sessão — agents project-specific exigem reload do Claude Code para serem indexados). Escopo reduzido conforme combinado com Luciano: 5 frentes em ~30-45min.

**Foco:** validar APENAS o que foi novo no dia 17/05/2026 — Bloco D (M7) + Mini-Sprint Bugs Usinas+Listas (M9) + Sub-Fase 1 Sprint Listas Concessionária Fases 1-3 (M10 parcial) + Mini-Bloco H'.9 HIBRIDO + fix tabela contratos.

---

## TL;DR (8 linhas)

Saúde técnica do código de produção está limpa: type check backend (excluindo scripts standalone) e frontend (1 erro pré-existente em `cooperados/[id]/page.tsx:691 protocoloConcessionaria`, não-relacionado) reportam **zero erros nas áreas novas**. Schema Prisma `validate` ✅. 11 rotas `/envios-lista/*` mapeadas no PM2. Frente 4 (smoke Prisma): 2 envios persistidos do smoke E2E (CANCELADA + HOMOLOGADO_TOTAL), Exfishes contrato ATIVO em 21.02%, `apelidoInterno @unique` ativo com 1 cooperebr2 só, `classeGdAnotada` exposto e null em todas as usinas. Bloco D ConfigTenant ✅ 7 chaves de cron seedadas em 2 cooperativas. **3 achados merecem atenção** mas nenhum é P0/P1 crítico — Fase 4 da Sub-Fase 1 pode prosseguir.

---

## Comandos executados

| # | Comando | Resultado |
|---|---|---|
| F1 | `Glob backend/src/envio-lista-concessionaria/**/*.ts` | 8 arquivos ✅ (controller + module + service + 5 DTOs) |
| F1 | `Glob web/components/envio-lista/*.tsx` | 7 arquivos ✅ (3 badges + 4 dialogs, sem DialogNovoEnvio deletado) |
| F1 | `Glob web/app/dashboard/listas-concessionaria/**/*.tsx` | 2 arquivos ✅ (`novo/page.tsx` + `[envioId]/page.tsx`) |
| F1 | `Glob memory/*17_05*.md` | 9 memórias ✅ (todas confirmadas presentes) |
| F1 | `ls ~/.claude/agents/cooperebr-qa-funcional.md` | ✅ existe |
| F2 | `pm2 logs cooperebr-backend --lines 300 -nostream \| grep envios-lista` | 11 rotas mapeadas (RouterExplorer) ✅ |
| F3a | `cd backend && npx tsc --noEmit` | **0 erros em `src/`**; 9 erros em `scripts/*` (pré-existentes, excluídos do build via `tsconfig.build.json`) ✅ |
| F3b | `cd web && npx tsc --noEmit` | **1 erro pré-existente** em `cooperados/[id]/page.tsx:691` (não-relacionado a 17/05) ✅ |
| F5 | `npx prisma validate` | "schema is valid 🚀" ✅ |
| F4 | `npx ts-node scripts/qa-leve-17-05.ts` (smoke Prisma) | 7/8 cenários OK + 1 achado D-30N (ver tabela) ✅ |

PM2: `cooperebr-backend` online (uptime ~46min, restarts 5 no dia), `cooperebr-whatsapp` online 2D. Portas 3000 + 3001 LISTENING.

---

## Frente 4 — Smoke Prisma (saída relevante)

```
envios_lista_concessionaria recentes: 2
  LIST-cooperebr2-202605-002 | status=HOMOLOGADO_TOTAL | created=2026-05-17T14:45:52
  LIST-cooperebr2-202605-001 | status=CANCELADA | created=2026-05-17T14:29:01

EnvioListaCooperado HOMOLOGADO: 1
  EXFISHES TERMINAL PESQUEIRO SPE LTDA | envio=LIST-cooperebr2-202605-002 (HOMOLOGADO_TOTAL) | dataHomologacao=2026-05-17

Usina.classeGdAnotada: 10/10 amostradas com null (esperado 0 anotações) ✅

Exfishes contrato vigente:
  CTR-2026-0010 | status=ATIVO | %=21.02 | kwh=2925.28
  usina: COOPERE - BR Usina 2 Linhares (cooperebr2)

usinas com apelidoInterno='cooperebr2': 1 (M9 saneamento ✅)

audit_log entries: 3 (mais recente 16/05 15:01 cooperado.criar)
  ⚠️  D-30N entries históricas, MAS sub-Fase 1 + Mini-Sprint 17/05 NÃO geraram novas entries

ConfigTenant Bloco D (cron_*): 7 chaves distintas seedadas:
  cron_alerta_admin_doc_ativo, cron_alerta_admin_doc_dias,
  cron_lembrete_doc_cooperado_ativo, cron_lembrete_doc_cooperado_horas,
  cron_lembrete_doc_cooperado_max_tentativas,
  cron_lembrete_email_edp_ativo, cron_lembrete_email_edp_horas

D-novo-I timezone check:
  raw UTC: 2026-05-17T14:45:52.716Z
  toLocaleString pt-BR default: 17/05/2026, 11:45:52
  toLocaleString pt-BR São Paulo: 17/05/2026, 11:45:52
  → 3h offset correto, datas batem; sintoma reportado não reproduzido com esses 2 envios.
```

---

## Tabela de achados

| # | Sev | Fluxo | Sintoma | Hipótese causa | Sugestão mínima |
|---|---|---|---|---|---|
| 1 | **P2** | AuditLog D-30N inativo em rotas novas | Mini-sprint 17/05 (PATCH `/usinas/:id`, `/migracoes-usina/ajustar-kwh`) + Sub-Fase 1 (POST `/envios-lista`, PATCHes diversos) **não geraram entries em `audit_log`**. Última entry registrada foi 16/05 15:01 `cooperado.criar`. | D-30N AuditLogInterceptor possivelmente registrado apenas em `cooperados` module, não global via `APP_INTERCEPTOR`. Confirma estado do `inventario_sisgd_completo_consolidado.md`: "D-30N = 0 writes" — interceptor inativo na maioria das rotas. | Promover AuditLogInterceptor a global em `app.module.ts` via `APP_INTERCEPTOR` provider. **Já catalogado como D-30N**. Não é regressão de 17/05, é estado conhecido. |
| 2 | **P3** | ConfigTenant chaves vs documentação | Doc-sessão Bloco D + memórias falam "**9 ConfigTenant chaves seedadas**". Banco tem **7 chaves distintas × 2 cooperativas = 14 entries** (`cron_*`). Discrepância numérica. | Provavelmente "9" se referia a 9 categorias semânticas ou contava `email.smtp.*`/`email.monitor.*` em conjunto. Documentação imprecisa, não bug funcional. | Atualizar fechamento Bloco D em memória `project_features_mar_abr.md` ou criar nota: "7 chaves cron × 2 cooperativas = 14 entries efetivas". |
| 3 | **P3** | D-novo-I timezone bug | Catalogado no fechamento como "Protocolada em 16/05 com hora 21:00 — 1 dia atrás". **Não reproduzido** nesta ronda com os 2 envios criados hoje (datas batem em UTC + Brasil). | Pode ser específico: envio criado em UTC 02:00+ vira "dia anterior" no formato local sem `timeZone` option no front. Não reproduzível sem entry específica no banco. | Investigar todos os `new Date(x).toLocaleDateString('pt-BR')` no frontend (especialmente em `[envioId]/page.tsx`) e padronizar com `{ timeZone: 'America/Sao_Paulo' }` ou usar `format` do `date-fns` com tz. Pode ser tentativa de fix preventivo sem repro confirmada. |

**Nenhum P0 ou P1 encontrado.** **Sub-Fase 1 Fase 4 LIBERADA pra prosseguir.**

---

## Riscos remanescentes

- **AuditLog D-30N inativo** — não é regressão, mas próximo onboarding parceiro real (Sinergia) sem trilha de auditoria é risco operacional/jurídico. Catalogar prioridade de promoção a global.
- **D-novo-H pendente** — convenção MENSAL × ANUAL em `migracoes-usina.service.ts:ajustarKwh` ainda divide kWh por 12 indevidamente. Usuário admin convive com workaround mental ("digite 22% e veja 3061 kWh em vez de 36740"). Fase 4 da Sub-Fase 1 NÃO depende disso, mas é ponto de fricção operacional.
- **Smoke E2E manual da Sub-Fase 1 Fase 3** foi 14/14 PASS conforme Luciano, mas tela `/dashboard/listas-concessionaria/novo` rodou apenas com 1 cooperado elegível (Exfishes em Cooperebr2). Não foi estressada com 50-100 cooperados (sintoma original do dialog apertado). Frontend dev em :3001 estável, mas escala ainda não validada.
- **Frontend dev em terminal vivo** — sem PM2 protegendo. Se Luciano fechar VS Code, frontend cai. Risco operacional pequeno mas conhecido.

---

## Próximos 3-5 fixes recomendados (priorizados)

1. **Prosseguir Sub-Fase 1 Fase 4** (trigger ativação + WA/email cooperado homologado, 2-3h) — está liberado, nenhum bloqueio encontrado. Aplicar `regra_contato_teste_impreterivel` no smoke.
2. **Promover AuditLogInterceptor a global** via `APP_INTERCEPTOR` em `app.module.ts` (P2, ~2h) — destrava observabilidade pra mini-sprint 17/05 + Sub-Fase 1 retroativamente em pull-forward.
3. **Sprint D-novo-H** (refator convenção MENSAL, ~6-8h, P1 estratégico) — decisão produto pré-aprovada, escopo cirúrgico, libera operação intuitiva de `ajustarKwh`/`migrarCooperado`.
4. **Atualizar fechamento Bloco D** com "7 chaves cron × 2 cooperativas" (correção docs minor, 15min).
5. **Sprint Usinas+Listas Sub-Fase 1 Fase 5** (tests Jest + docs + commits push, 3-5h) — fecha M10 completo.

---

## Decisão recomendada pra próxima sessão

✅ **Aplicar Fase 4 da Sub-Fase 1** (trigger ativação + WA/email) — sem bloqueio. Nenhum P0/P1.

⚠️ **Antes:** se Luciano quiser cobertura QA mais ampla, reload do Claude Code (`/exit` e relogar) carrega subagent project-specific `cooperebr-qa-funcional` corretamente. Aí dispara QA completo com 3 Missões (incluindo `npm test` + `npm run build`) cobrindo o projeto inteiro.

---

## Limitações desta ronda piloto

- **NÃO** rodei `npm test` ou `npm run build` (consome tokens demais — limitado a tsc --noEmit)
- **NÃO** testei fluxos via Playwright/Vercel
- **NÃO** validei comunicação real (WA/email/Asaas)
- **NÃO** stress-testei criação de envio com 50+ cooperados
- Banco em estado pós-smoke E2E Fase 3 (2 envios persistidos) — não foi limpo nesta ronda

---

## Anexos

- Script `backend/scripts/qa-leve-17-05.ts` (read-only) — pode rodar novamente em sessões futuras com `npx ts-node scripts/qa-leve-17-05.ts`
- Subagent disponível em `~/.claude/agents/cooperebr-qa-funcional.md` (carrega no próximo reload do Claude Code)

---

**Origem:** main thread, sessão Code 17/05/2026 noite final (pós-fechamento maratona em commit `787f0f9`).
