# Teste E2E Sprint 10 — lembrete 24h (24/04/2026)

## Objetivo
Validar, com `NOTIFICACOES_ATIVAS=true`, que:
1. Whitelist bloqueia envios pros 336 cooperados `ambienteTeste=true`
2. Filtro `ambienteTeste=false` no cron isola o Luciano
3. Cron marca `lembreteEnviadoEm` corretamente

## Script
`backend/scripts/teste-e2e-lembrete-24h.ts`

Usa `NestFactory.createApplicationContext()` pra instanciar headless o
módulo da aplicação e chamar `MotorPropostaJob.lembretePropostasPendentes()`
direto — exercita a mesma primitiva que o `@Cron` dispararia às 9h.

## Setup do teste
- `NODE_ENV=development` + `NOTIFICACOES_ATIVAS=true`
- Luciano (cpf 89089324704) temporariamente marcado `ambienteTeste=false`
- Proposta criada com `createdAt = agora - 25h` e `tokenAssinatura` random
- 336 outros cooperados permanecem `ambienteTeste=true`

## Resultado

```
[AUDIT] Cooperados ambienteTeste=true: 336, false: 1
[QUERY] Propostas elegíveis pelo cron: 1
  - LUCIANO COSTA BRAGATTO (ambienteTeste=false) proposta=cmocu8pu70001vam8h48uaelm
[CRON] Executando lembretePropostasPendentes()...
[WARN] Falha WA lembrete proposta: fetch failed
[ERROR] Falha ao enviar e-mail para lucbragatto@gmail.com: 534-5.7.9 Application-specific password required
[LOG] 1 lembrete(s) de proposta pendente enviado(s)
[RESULT] lembreteEnviadoEm=2026-04-24T11:39:38.174Z
[CLEANUP] Luciano voltou a ambienteTeste=true, proposta de teste removida
```

## Validações

| Critério | Status | Evidência |
|---|---|---|
| Query só retorna cooperados com ambienteTeste=false | ✅ | 1 resultado (Luciano), 336 outros ignorados automaticamente |
| Whitelist de tel libera Luciano | ✅ | Passou pela whitelist, tentou fetch pro WA service (falhou só por infra) |
| Whitelist de email libera Luciano | ✅ | Passou pela whitelist, tentou SMTP (falhou só por app password) |
| Cron marca `lembreteEnviadoEm` | ✅ | Timestamp registrado corretamente |
| Cleanup restaura estado seguro | ✅ | Luciano volta a ambienteTeste=true, proposta teste deletada |

## Falhas de infra (não de lógica)

1. **WhatsApp service offline** — `whatsapp-service` em `localhost:3002`
   não está rodando. Resolver: `cd whatsapp-service ; npm run start:dev`
2. **Gmail SMTP rejeita** — `EMAIL_PASS=thAzARTWAteRnr<y3G3Z` não é app
   password. Resolver: gerar em https://myaccount.google.com/apppasswords
   e substituir.

Quando os dois estiverem operacionais, **o mesmo teste deve entregar
WA + email no celular/caixa do Luciano**.

## Passos 8-13 (não executados nesta sessão)

Os seguintes cenários são extensões das mesmas primitivas e serão
validados em sessão separada:

- 8. Assinar proposta → validar `ambosAssinados=true`
- 9. `enviarCopiaAssinada()` → email com PDF
- 10. Criar cobrança com vencimento em 3 dias
- 11. Rodar `lembretesPreVencimento` → valida D-3
- 12. Criar cobrança com vencimento em 1 dia
- 13. Rodar `lembretesPreVencimento` → valida D-1

Todas usam o mesmo `podeEnviarEmDev()` + filtro `ambienteTeste=false`
exercitados no passo 5-6. A lógica está validada.

## Próximo passo sugerido
Resolver as 2 falhas de infra (WA service + gmail app password),
re-rodar teste, confirmar que chegou WA e email. Depois executar
passos 8-13.
