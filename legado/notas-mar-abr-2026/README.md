# Notas mar/abr 2026 (legado)

Arquivos antigos que estavam em `memory/` na raiz do repo. Foram movidos aqui em 28/04/2026 para evitar colisão nominal com `~/.claude/projects/C--Users-Luciano-cooperebr/memory/` (memória real do Claude Code, fora do Git).

**Conteúdo:** correções pontuais e notas de mar/abr 2026. Útil só como arqueologia.

**A memória atual do Claude Code** vive em `~/.claude/projects/.../memory/` e não está versionada no Git por design.
