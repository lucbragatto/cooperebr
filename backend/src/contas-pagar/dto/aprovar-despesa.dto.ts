/**
 * D-novo-BH (M37, 29/05/2026) — DTO de aprovação de despesa proposta.
 *
 * Corpo vazio por design — ação simples (admin clica "Aprovar").
 * Identidade do admin vem de req.user. Audit log via @AuditLog decorator.
 */
export class AprovarDespesaDto {}
